{
  "name": "@danielpickett/components",
  "version": "0.0.0",
  "private": true,
  "types": "dist/types.d.ts",
  "source": "src/index.ts",
  "alias": {
    "src": "./src"
  },
  "browserModern": "dist/index.js",
  "targets": {
    "browserModern": {
      "engines": {
        "browsers": [
          "last 1 Chrome version"
        ]
      }
    }
  },
  "scripts": {
    "start": "rm -rf dist && parcel watch --no-cache",
    "build": "rm -rf dist && parcel build --no-cache"
  },
  "dependencies": {
    "@danielpickett/hooks": "0.0.0",
    "@danielpickett/types": "0.0.0",
    "@types/react": "^18.0.18",
    "@types/react-dom": "^18.0.6",
    "classnames": "^2.3.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@parcel/transformer-typescript-tsc": "^2.7.0",
    "parcel": "^2.7.0",
    "typescript": ">=3.0.0"
  }
}
